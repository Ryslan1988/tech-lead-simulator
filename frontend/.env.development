# Dev defaults. Requests go to /api; MSW intercepts them in the browser.
VITE_API_BASE=/api
# "true" enables the MSW mock backend; set to "false" to hit the real
# backend through the Vite dev proxy (see vite.config.ts).
VITE_USE_MOCKS=true
