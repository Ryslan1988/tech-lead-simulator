# openapi-typescript (dev-only codegen) still pins peer typescript@^5.x and
# hasn't widened its range to TS 6, which the project uses. It's forward-
# compatible in practice, so relax peer resolution for installs here.
legacy-peer-deps=true